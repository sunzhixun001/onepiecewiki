<navbar>
  <navigation-bar
    loading="{{true}}" 
    show="{{true}}" 
    animated="{{false}}" 
    background="{{background}}"
    color="{{color}}" 
    title="{{title}}" 
    back="{{back}}"
  />
</navbar>
