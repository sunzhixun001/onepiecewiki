<view class='nav-wrap' style='height:{{statusBarHeight}}px'>
  <view class='nav-content'>
    <back wx:if="{{showCapsule && !share}}" />
    <view class='nav-title'><text>{{title || '海贼王大百科'}}</text></view>
  </view>
</view>
