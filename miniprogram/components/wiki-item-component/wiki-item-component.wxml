<navigator url="{{url}}" class="li">
  <view class="img">
    <image src="{{img}}" mode="widthFix" bindload="onImageLoad"/>
  </view>
  <view class="detail">
    <view class="title">{{title}}</view>
  </view>
</navigator>
