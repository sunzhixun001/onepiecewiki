<block>
  <navbar />
  <view class="container" style='margin-top: {{statusBarHeight}}px'>
    <view class="main">
      <view class="wrap">
        <view class="banner-area">
          <swiper
            previous-margin='30px'
            next-margin="70px"
            circular
          >
            <swiper-item
              wx:for="{{banners}}"
              wx:key="_id"
            >
              <view class="item-container">
                <view class="img" bindtap="switchTimelineTab">
                  <image 
                    src="{{item.url}}" 
                    mode="widthFix"
                  />
                </view>
                <view class='title'>{{item.title}}</view>
                <view class="years">{{item.year}}</view>
              </view>
            </swiper-item>
          </swiper>
        </view>
        <view class="water-area">
          <view class="li" wx:for="{{storys}}" wx:key="_id">
            <view class="img" style="height:{{item.height}}rpx">
              <image src="{{item.url}}" mode="widthFix"/>
            </view>
            <view class="detail">
              <view class="title">{{item.title}}</view>
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>
</block>
