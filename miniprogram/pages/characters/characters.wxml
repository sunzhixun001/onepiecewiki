<block>
  <navbar 
    title="人物"
    back="{{false}}"
    background="#D9312B"
    color="#FFFFFF"
  />   
  <!-- <view class='nav-wrap' style='height:{{statusBarHeight}}px'>
    <view class='nav-content'>
      <view class='nav-head'>
        <view bindtap="bindSearchIcon" wx:if="{{!searchActive}}">
          <text class='fa fa-search'/>
        </view>
        <view bindtap="bindSearchCloseIcon" wx:else>
          <text class='fa fa-close'/>
        </view>
      </view>
      <view class='nav-title'><text>海贼王人物</text></view>
    </view>
    <view class='mainTitleArea bgGray {{searchActive?"":"disabled"}}'>
        <view class='searchDetailArea'>
          <view class='inner'>
            <input 
              placeholder='路飞' 
              bindinput='bindSearchInput' 
              value='{{keyword}}'
              confirm-type="搜索"
              bindconfirm="bindJumpSearch"
            />
          </view>
        </view>
        <view class='searchIcon'>
          <icon bindtap='bindJumpSearch' type="search" size="16" color='#3D3D3D'/>
        </view>
      </view>
  </view> -->
  <view class='container'>
    <view class='main listPage'>
      <view class='bgGray borderBottom tabListArea'>
        <view class='swiper-container headerTabList'>
          <view class='swiper-wrapper'>
            <view 
              class="swiper-slide item {{item.index === currentIndex ? 'on':''}}" 
              wx:for="{{tabs}}" 
              wx:key="index"
              data-index="{{item.index}}"
              bindtap="bindTabChange"
            >
              <text>{{item.name}}</text>
            </view>
          </view>
        </view>
      </view>
      <view class='cw'>
        <view class='listArea'>
            <swiper
              class="swiper"
              bindchange='bindSwiperChange'
              current="{{currentIndex}}"
            >
              <swiper-item>
                <character-list 
                  items="{{strawHatCharacters.data}}"
                  total="{{strawHatCharacters.total}}"
                />
              </swiper-item>
              <swiper-item>
                <character-list 
                  items="{{pirates.data}}"
                  total="{{pirates.total}}"
                  bind:scrolltolower="piratesscrolltolower"
                />
              </swiper-item>
              <swiper-item>
                <character-list 
                  items="{{marines.data}}"
                  total="{{marines.total}}"
                  bind:scrolltolower="marinescrolltolower"
                />
              </swiper-item>
              <swiper-item>
                <character-list 
                  items="{{antagonists.data}}"
                  total="{{antagonists.total}}"
                  bind:scrolltolower="antagonistsscrolltolower"
                />
              </swiper-item>
            </swiper>
        </view>
      </view>
    </view>
  </view>
</block>